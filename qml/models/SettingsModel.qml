import QtQuick 2.9
import Ubuntu.Components 1.3
import Qt.labs.settings 1.0

Settings {
    // DEPRECATED
    property var token
    property var server
    property var username
    property var matrixid
    property var id_server
    property var deviceID
    property var deviceName
    property var matrixVersions
    property var lazy_load_members
    property var since
    property var sendTypingNotification
    property var hideLessImportantEvents
    property var autoloadGifs
    property var countryCode
    property var countryTel
}
